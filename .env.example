# Local-only values. Generate a unique password; never use production credentials.
POSTGRES_PASSWORD=
DIRECT_URL=
# Build-time public values. Leave empty until the API and Turnstile are deployed.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
